# Security

Vega Nexus is a router, not a provider client. The MVP does not require API keys and should not store secrets. Report issues involving secret leakage, unsafe config handling, or misleading routing claims.
